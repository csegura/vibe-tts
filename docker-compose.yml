services:
  vibe-tts:
    build: .
    ports:
      - "8000:8000"
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - VIBE_DEVICE=${VIBE_DEVICE:-auto}
      - VIBE_MODEL=${VIBE_MODEL:-vibe-1.5b}
    # Uncomment for GPU support:
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    # Optional: persist model cache
    # volumes:
    #   - vibe-tts-cache:/root/.cache/huggingface

# volumes:
#   vibe-tts-cache:
