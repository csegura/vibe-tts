services:
  vibe-tts:
    build: .
    ports:
      - "8000:8000"
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    # Uncomment for GPU support:
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    command: ["vibe-tts", "serve", "--host", "0.0.0.0", "--port", "8000", "--device", "auto"]
    # Optional: persist model cache
    # volumes:
    #   - vibe-tts-cache:/root/.cache/huggingface

# volumes:
#   vibe-tts-cache:
